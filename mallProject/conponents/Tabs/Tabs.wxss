.Tabs_title{
  display: flex;
  
  
}
.Tabs_title_item{
  /* width: 33.33%; */
  display: flex;
  flex:1;
  justify-content:center;
  align-items: center;
  padding: 15rpx 0;
}
.active{
  color: var(--themeColor);
  border-bottom:5rpx solid var(--themeColor);
}